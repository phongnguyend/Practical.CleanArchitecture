apiVersion: v1
kind: Service
metadata:
  labels:
    app: {{ .Release.Name}}-identity-grpc
  name: {{ .Release.Name}}-identity-grpc
spec:
  ports:
  - name: "443"
    port: 443
    targetPort: 443
  selector:
    app: {{ .Release.Name}}-identity-grpc
status:
  loadBalancer: {}
