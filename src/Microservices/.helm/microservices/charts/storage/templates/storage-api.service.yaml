apiVersion: v1
kind: Service
metadata:
  labels:
    app: {{ .Release.Name}}-storage-api
  name: {{ .Release.Name}}-storage-api
spec:
  ports:
  - name: "80"
    port: 80
    targetPort: 80
  selector:
    app: {{ .Release.Name}}-storage-api
status:
  loadBalancer: {}
